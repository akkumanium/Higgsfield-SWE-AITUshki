{
  "name": "@ai-canvas/backend",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test:planner-invariant": "pnpm run build && node dist/agent/geminiClient.invariant.test.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ai-canvas/shared": "workspace:*"
  }
}
