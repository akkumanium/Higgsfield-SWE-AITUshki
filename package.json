{
  "name": "ai-canvas-mvp",
  "private": true,
  "scripts": {
    "predev": "node ./scripts/kill-dev-ports.mjs",
    "build": "tsc -b app/shared app/frontend app/backend app/sync-server app/e2e",
    "check": "tsc -b --pretty false",
    "smoke": "pnpm --filter @ai-canvas/e2e smoke",
    "dev": "pnpm run build && concurrently -k \"pnpm --filter @ai-canvas/backend start\" \"pnpm --filter @ai-canvas/sync-server start\" \"pnpm --filter @ai-canvas/frontend dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.0.0"
  }
}
